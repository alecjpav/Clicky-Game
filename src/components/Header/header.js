import React from 'react'
import './header.css'

const Header = () => {
	return (
		<header className="header">
			<h1>Clicky Game - Don't click the same picture twice!</h1> 
			
		</header>
	)
}

export default Header